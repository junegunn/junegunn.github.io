---
title: '0.68.0'
weight: -680
---

# [0.68.0][0.68.0]

(Feb 2026)

[0.68.0]: https://github.com/junegunn/fzf/releases/tag/v0.68.0

## Quick summary

This release introduces word wrapping for both the list and preview sections,
underline style variants, and fish shell improvements.

| Type        | Class   | Name                        | Description                                          |
| :--         | :--     | :--                         | :--                                                  |
| New         | Option  | `--wrap=word`               | Word-level wrapping in the list section              |
| New         | Option  | `--preview-window wrap-word`| Word-level wrapping in the preview window            |
| New         | Option  | `--preview-wrap-sign`       | Wrap indicator for the preview window                |
| New         | Action  | `toggle-wrap-word`          | Toggle word wrapping in the list section             |
| New         | Action  | `toggle-preview-wrap-word`  | Toggle word wrapping in the preview window           |
| New         | Color   | `underline-*`               | Underline style variants for `--color`               |
| New         | Color   | `alt-gutter`                | Alternate gutter color                               |
| New         | Env Var | `$FZF_WRAP`                 | Wrapping mode exported to child processes            |
| Enhancement | Shell   | fish `CTRL-R`               | Syntax highlighting and preview                      |
| New         | Shell   | fish completion             | Fish completion support (SHIFT-TAB)                  |

## 1. Word wrapping

### List section

fzf now supports word-level line wrapping in the list section with `--wrap=word`
(or `--wrap-word`). The default bindings for `ctrl-/` and `alt-/` have been
changed from `toggle-wrap` to `toggle-wrap-word`.

```sh
fzf --wrap=word
```

### Preview window

Word-level wrapping is also available in the preview window with the
`wrap-word` flag and `toggle-preview-wrap-word` action.

```sh
fzf --preview 'bat --style=plain --color=always {}' \
    --preview-window wrap-word \
    --bind space:toggle-preview-wrap-word
```

A new `--preview-wrap-sign` option lets you set a different wrap indicator for
the preview window.

## 2. Underline style variants

`--color` now supports underline style variants: `underline-double`,
`underline-curly`, `underline-dotted`, and `underline-dashed`.

```sh
fzf --color 'fg:underline-curly,current-fg:underline-dashed'
```

fzf also now supports underline styles (`4:N`) and underline colors (SGR 58/59)
in ANSI input.

```sh
# In the list section
printf '\e[4:3;58;2;255;0;0mRed curly underline\e[0m\n' | fzf --ansi

# In the preview window
fzf --preview "printf '\e[4:3;58;2;255;0;0mRed curly underline\e[0m\n'"
```

## 3. New color and environment variable

- `alt-gutter` color option
  ([#4602](https://github.com/junegunn/fzf/pull/4602))
  ([@hedgieinsocks](https://github.com/hedgieinsocks))
- `$FZF_WRAP` environment variable is now exported to child processes
  (`char` or `word` when wrapping is enabled)
  ([#4672](https://github.com/junegunn/fzf/pull/4672))
  ([@bitraid](https://github.com/bitraid))

## 4. Shell integration improvements

### fish

Thanks to [@bitraid](https://github.com/bitraid), fish `CTRL-R` has been
significantly improved
([#4672](https://github.com/junegunn/fzf/pull/4672)):

- Syntax highlighting in the list on fish 4.3.3+
- Syntax-highlighted preview window that auto-shows for long or multi-line
  commands
- `alt-enter` to reformat and insert selected commands
- Improved handling of bulk deletion of selected history entries
  (`shift-delete`)

Fish completion support (`SHIFT-TAB`) has also been added
([#4605](https://github.com/junegunn/fzf/pull/4605))
([@lalvarezt](https://github.com/lalvarezt)).

### zsh

- Handle multi-line history selection
  ([#4595](https://github.com/junegunn/fzf/pull/4595))
  ([@LangLangBart](https://github.com/LangLangBart))

## Bug fixes

- Fixed `_fzf_compgen_{path,dir}` to respect `FZF_COMPLETION_{PATH,DIR}_OPTS`
  ([#4592](https://github.com/junegunn/fzf/pull/4592))
  ([@shtse8](https://github.com/shtse8), [@LangLangBart](https://github.com/LangLangBart))
- Fixed `--preview-window follow` not working correctly with wrapping
  ([#3243](https://github.com/junegunn/fzf/issues/3243),
  [#4258](https://github.com/junegunn/fzf/issues/4258))
- Fixed symlinks to directories being returned as files
  ([#4676](https://github.com/junegunn/fzf/pull/4676))
  ([@skk64](https://github.com/skk64))
- Fixed SIGHUP signal handling
  ([#4668](https://github.com/junegunn/fzf/pull/4668))
  ([@LangLangBart](https://github.com/LangLangBart))
- Fixed preview process not killed on exit
  ([#4667](https://github.com/junegunn/fzf/pull/4667))
- Fixed coloring of items with zero-width characters
  ([#4620](https://github.com/junegunn/fzf/pull/4620))
- Fixed `track-current` unset after a combined movement action
  ([#4649](https://github.com/junegunn/fzf/pull/4649))
- Fixed `--accept-nth` being ignored in filter mode
  ([#4636](https://github.com/junegunn/fzf/pull/4636))
  ([@charemma](https://github.com/charemma))
- Fixed display width calculation with `maxWidth`
  ([#4596](https://github.com/junegunn/fzf/pull/4596))
  ([@LangLangBart](https://github.com/LangLangBart))
- Fixed clearing of the rest of the current line on start
  ([#4652](https://github.com/junegunn/fzf/pull/4652))
- Fixed `x-api-key` header not required for GET requests
  ([#4627](https://github.com/junegunn/fzf/pull/4627))
- Fixed key reading not cancelled when `execute` triggered via a server request
  ([#4653](https://github.com/junegunn/fzf/pull/4653))
- Fixed rebind of readline command `redraw-current-line`
  ([#4635](https://github.com/junegunn/fzf/pull/4635))
  ([@jameslazo](https://github.com/jameslazo))
- Fixed `fzf-tmux` `TERM` quoting and added `mktemp` usage
  ([#4664](https://github.com/junegunn/fzf/pull/4664))
  ([@Goofygiraffe06](https://github.com/Goofygiraffe06))
- Do not allow very long queries in `FuzzyMatchV2`
  ([#4608](https://github.com/junegunn/fzf/pull/4608))
